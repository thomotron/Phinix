namespace Utils
{
    /// <summary>
    /// A common severity level enum for use with <c>LogEventArgs</c>.
    /// </summary>
    public enum LogLevel
    {
        DEBUG,
        INFO,
        WARNING,
        ERROR,
        FATAL
    }
}