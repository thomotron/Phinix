namespace Utils
{
    /// <summary>
    /// A common severity level enum for use with <see cref="LogEventArgs"/>.
    /// </summary>
    public enum LogLevel
    {
        DEBUG,
        INFO,
        WARNING,
        ERROR,
        FATAL
    }
}